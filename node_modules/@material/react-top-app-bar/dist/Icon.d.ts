import React from 'react';
export interface IconProps<T> extends React.HTMLProps<T> {
    actionItem?: boolean;
    className?: string;
    children: React.ReactElement<any>;
    navIcon?: boolean;
}
declare const Icon: <T extends Element = HTMLElement>(props: IconProps<T>) => React.ReactElement<HTMLElement>;
export default Icon;
