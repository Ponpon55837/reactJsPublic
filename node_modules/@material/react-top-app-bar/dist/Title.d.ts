import React from 'react';
export interface TitleProps<T> extends React.HTMLProps<T> {
    className?: string;
    tag?: string;
}
declare const Title: <T extends HTMLElement = HTMLSpanElement>(props: TitleProps<T>) => React.ReactElement<T>;
export default Title;
